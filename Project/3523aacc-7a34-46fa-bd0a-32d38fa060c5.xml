<StructuredTextModel xmlns="http://schemas.datacontract.org/2004/07/Omron.Cxap.Modules.StructuredText.Core" xmlns:i="http://www.w3.org/2001/XMLSchema-instance"><Text>IF NOT ISEL_R THEN  // ← Changed this line!&#xD;
    &#xD;
    (* Generate fake analog input for flow sensor *)&#xD;
    iSimAnalog := REAL_TO_INT(((rSimFlow - 0.05) / 1.45) * 26214.0 + 6553.0);&#xD;
    ai_Flow_4to20mA := iSimAnalog;&#xD;
    &#xD;
    (* Generate fake IO-Link data for pressure sensor *)&#xD;
    wSimPressureRaw := REAL_TO_WORD((rSimPressure / 10.0) * 65535.0);&#xD;
    awPressure_Data[0] := WORD_TO_BYTE(SHR(wSimPressureRaw, 8));&#xD;
    awPressure_Data[1] := WORD_TO_BYTE(wSimPressureRaw);&#xD;
    &#xD;
    (* Slowly change values *)&#xD;
    rSimFlow := rSimFlow + 0.02;&#xD;
    IF rSimFlow &gt; 1.5 THEN&#xD;
        rSimFlow := 0.05;&#xD;
    END_IF;&#xD;
    &#xD;
    rSimPressure := rSimPressure + 0.15;&#xD;
    IF rSimPressure &gt; 10.0 THEN&#xD;
        rSimPressure := 0.5;&#xD;
    END_IF;&#xD;
    &#xD;
END_IF;</Text></StructuredTextModel>